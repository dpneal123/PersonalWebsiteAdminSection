@model IEnumerable<WebApplication1.Models.Framework>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FrameworkName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FrameworkIconLoc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FrameworkName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FrameworkIconLoc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FrameworkId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FrameworkId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FrameworkId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
