@model IEnumerable<WebApplication1.Models.ProjectFramework>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Project)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Framework)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Project.ProjectName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Framework.FrameworkName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProjFrameId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProjFrameId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProjFrameId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
